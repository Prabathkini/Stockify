export type stockTableProps = {
    id: number
    stockName: string
    symbol: string
    suggestion: string
  }

  export type cryptoTableProps ={
    id: number
    cryptoName: string
    symbol: string
    suggestion: string
  }